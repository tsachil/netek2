services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: btcts
      POSTGRES_PASSWORD: btcts
      POSTGRES_DB: btcts
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
